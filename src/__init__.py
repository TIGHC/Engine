# Intentionally empty - this file only exists to make src/ an explicit
# package, so `from src import tighc` / `from src.tighc import NAME`
# resolve correctly from gui.py. See src/tighc.py for what's
# actually re-exported from this package.
